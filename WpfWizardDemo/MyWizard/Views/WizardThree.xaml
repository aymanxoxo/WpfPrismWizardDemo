<UserControl x:Class="WpfWizardDemo.MyWizard.Views.WizardThree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="44" Text="{Binding HelloText}"/>
        <Button HorizontalAlignment="Left" Grid.Row="1" Content="Previous" Command="{Binding PrevCommand}"/>
        <Button HorizontalAlignment="Right" Grid.Row="1" Content="Close" Command="{Binding DoneCommand}"/>
    </Grid>
</UserControl>
